// Jenkinsfile
@Library('desafio2') _

pipeline {
    agent none
    stages {
        stage ('Example') {
            steps {
                // log.info 'Starting' 
                script { 
                    echo 'example'
                }
            }
        }
    }
}